import React from 'react';

import {NavLink} from 'react-router-dom';

const Navigation = () => {
  
  return (
    <div>
    
    <NavLink to="/">H</NavLink>
    <NavLink to="/about">A</NavLink>
    <NavLink to="/contact">C</NavLink>
    </div>
  )
}

export default Navigation;